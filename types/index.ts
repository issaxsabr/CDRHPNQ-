/**
 * Point d'entrée pour tous les types de l'application
 */

// Re-export des types principaux
export * from './api';

// Note: Les types de base (BusinessData, Project, etc.) sont dans ../types.ts
// pour des raisons de rétrocompatibilité
